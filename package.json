{
	"name": "blog-manager",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"dev": "concurrently \"npm --prefix api run dev\" \"npm --prefix frontend run dev\"",
		"dev:api": "npm --prefix api run dev",
		"dev:frontend": "npm --prefix frontend run dev"
	},
	"devDependencies": {
		"concurrently": "^8.2.0"
	}
}
