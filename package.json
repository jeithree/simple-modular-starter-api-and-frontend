{
	"name": "blog-manager",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"dev": "concurrently \"npm --prefix api run dev\" \"npm --prefix frontend run dev\"",
		"dev:api": "npm --prefix api run dev",
		"dev:frontend": "npm --prefix frontend run dev",
		"type-check": "npm --prefix api run type-check && npm --prefix frontend exec tsc -- --noEmit",
		"lint": "npm --prefix frontend run lint",
		"build": "npm --prefix frontend run build"
	},
	"devDependencies": {
		"concurrently": "^8.2.0"
	}
}
